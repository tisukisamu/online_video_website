server:
  port: 8080

spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://47.97.108.222:3306/gp2?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: kimi
    password: tR!9dB2x@7QwLpZ$mF8s
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

jwt:
  secret: mySecretKeyForJwtTokenGeneration2024MySecretKey
  expiration: 86400000

upload:
  path: ./uploads
  video:
    max-size: 524288000
  image:
    max-size: 10485760

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.backend: DEBUG
